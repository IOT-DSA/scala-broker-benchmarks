#!/usr/bin/env bash

DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"

env \
  "broker.url=http://localhost:8080/conn" \
  "requester.instances=100" \
  "requester.name=bm-req" \
  "requester.batch=20" \
  "responder.instances=1" \
  "responder.nodes=20" \
  "responder.name=bm-rsp" \
  "responder.autoinc.interval=500" \
  "responder.autoinc.nodes=5" \
  $DIR/../target/universal/stage/bin/auto-inc-no-invoke-app
