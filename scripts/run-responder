#!/usr/bin/env bash

DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"

env \
  "broker.url=http://localhost:8080/conn" \
  "stats.interval=20000" \
  "responder.instances=5" \
  "responder.nodes=10" \
  "responder.name=bm-rsp" \
  "responder.autoinc.interval=1000" \
  "responder.autoinc.nodes=5" \
  $DIR/../target/universal/stage/bin/benchmark-responder-app
